import { TagDescription } from "../models/schemas";
import { RecordingRel } from "../models/relschemas";

export const ResultService = {
  async create(recording_id: number): Promise<void> {
    return;
  },
};
